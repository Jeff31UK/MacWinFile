import Cocoa

enum WinFileFonts {
    static let fileList = NSFont.monospacedSystemFont(ofSize: 11, weight: .regular)
    static let treeView = NSFont.systemFont(ofSize: 12)
    static let statusBar = NSFont.systemFont(ofSize: 11)
    static let titleBar = NSFont.boldSystemFont(ofSize: 11)
    static let toolbar = NSFont.systemFont(ofSize: 10)
    static let columnHeader = NSFont.boldSystemFont(ofSize: 10)
}
